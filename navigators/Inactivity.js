import React, { useState, useRef, useEffect } from "react";



const Inactivty = ({showInactivity, setShowInactivity}) => {
  

    console.log(showInactivity)

    

        useEffect(() => {
                // if(value)
                // setActive(false)
        }, [value])

        // UserInactivity
        //     isActive={active}
        //     timeForInactivity={timer}
        //     onAction={isActive => {
        //         console.log("isActive", isActive)
        //         // setActive(isActive);
        //     }}
        //     style={{ flex: 1, paddingTop: '10%' }}
        

    return (
        <>
            
        </>
    )
}

export default Inactivty